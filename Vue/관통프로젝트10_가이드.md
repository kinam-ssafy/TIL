# 관통 프로젝트 10 가이드

## 📚 목차
1. 프로젝트 기획과 설계
2. 데이터 가공 및 저장
3. 참고: Q 객체

---

## 1️⃣ 프로젝트 기획과 설계

### 개요

#### 기획(Planning)이란?
**프로젝트 목표와 전략을 수립하는 과정**

#### 설계(Design)란?
**기획된 목표를 구현하기 위한 구조와 방법을 정의하는 과정**

---

### 요구사항 분석

**사용자와 이해 관계자의 필요 요구를 수집하고 명세화하는 과정이 필요**

#### 요구사항 분석의 목적
- **"무엇을", "어떻게"** 해야 하는지를 정의
- 기획 단계에서 설정된 목표를 구체적 실행 계획으로 전환
- 설계 단계의 정확성과 효율성 확보

#### 요구사항의 종류

**1. 기능적 요구사항**
- "회원은 이메일로 가입할 수 있어야 한다"
- "상품 목록을 페이지네이션해서 보여줘야 한다"

**2. 비기능적 요구사항**
- "초기 로딩 시간은 3초 이내여야 한다"

---

### 요구사항 분석을 위해서

#### 1) 핵심 기능 식별
- **'시스템이 꼭 해야할 기능'**과 **'추가될 수 있는 기능'**을 구분
- 기능 우선순위를 매겨, 무엇부터 구현할지 결정

#### 2) 사용자 정의
- 사용자 유형 (페르소나)을 설정
- 그들이 서비스를 어떻게 사용할지(사용자 여정)를 파악

#### 3) 제약 사항 파악
- 기술(사용할 스택)
- 일정(개발 기간)
- 예산
- 법적 요건 등

---

### 아키텍처와 기술 스택 결정

**선택지:**
- Django (Template with Vue?)
- DRF + Vue?

#### 아키텍처 결정의 의미
1. **유지 보수성과 확장성 확보**
2. **팀 역할 분담 명확화**

#### 스택 선택 기준
1. **학습 곡선**
2. **커뮤니티 지원**
3. **성능**

---

### 라이브러리 선정 (Vue 기준)

**우리 프로젝트에 Vue Router 도입이 필수적일까?**

**고려사항:**
- 페이지 전환 빈도는 어느 정도인가?
- 화면간 이동이 많고 URL 공유 기능 등을 구현하고자 한다면?
- 동적 중첩 라우팅 기술이 필요한가?

---

### 프로젝트 구조 및 코드 컨벤션

**목적:**
- 코드 가독성, 유지보수성을 확보
- 팀원 간 협업 시 혼선을 최소화하여 업무의 효율화

#### 프로젝트 구조

**Django 프로젝트:**
- `views.py`, `models.py`, `urls.py`, `serializers.py` 등

**Vue 프로젝트:**
- `components/`, `views/`, `stores/`, `router/`, `assets/` 등

**외에도 필요에 따라 각 기능별(app 별)로 폴더의 구조화 작업**

---

### 네이밍 컨벤션

**중요성:**
- 요청에 대한 응답 데이터의 네이밍 컨벤션을 준수하여 업무 효율성 증가
- 컴포넌트 명 작성 방식의 권장안을 정하여 협업 시 작업 편의성 증가

**핵심 포인트: 일관된 스타일 유지**

---

### Git Commit 컨벤션

```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정 (README)
style: 코드 포맷 수정 (코드 동작에 영향 없음)
refactor: 코드 구조 개선
```

---

## 2️⃣ Vue 프로젝트 세부 설계

### Vue 프로젝트 세부 설계 과정

1. **라우팅 설계**
2. **컴포넌트 분리 및 책임 정의**
3. **API 설계 및 연동 구조**
4. **UI/UX 스타일 가이드**
5. **Git flow**
6. **문서화**

---

## 3️⃣ 라우팅 설계

### 라우팅 설계 (1/4)

**기능 구현에 필요한 최소한의 Route Tree 구상**

#### 고려사항
- **중첩 라우팅(Nested) vs 단일 라우팅 결정**
- **라우터 가드 전략 세우기**
- **RESTful URL 설계**

#### Vue Router Tree 예시

```
/
├── Users
│   └── UsersLayout
│       ├── List
│       ├── Detail (:id)
│       └── Create
├── About
├── AdminDashboard
│   └── settings
└── NotFound
```

---

### 라우팅 설계 (2/4): 중첩 라우팅

**URL에서의 중첩된 관계를 표현하는 관점**

```javascript
{
  path: '/user/:id',
  children: [
    {
      path: 'profile',  // /user/:id/profile
      name: 'user-profile',
      component: UserProfile
    },
    {
      path: 'posts',    // /user/:id/posts
      name: 'user-posts',
      component: UserPosts
    }
  ]
}
```

**애플리케이션의 UI는 여러 레벨 깊이로 중첩된 컴포넌트로 구성되기도 함**
- 이 경우 URL을 중첩된 컴포넌트의 구조에 따라 변경되도록 이 관계를 표현할 수 있음

---

### 라우팅 설계 (3/4): Navigation Guard

**Vue router를 통해 특정 URL에 접근할 때**
- 다른 URL로 redirect를 하거나 취소하여 내비게이션을 보호
- 라우트 전환 전/후 자동으로 실행되는 Hook

#### Navigation Guard 종류

**1. Globally (전역 가드)**
- 전역에서 모든 라우트 전환에 적용되는 가드

**2. Per-route (라우터 가드)**
- 특정 라우트에만 적용되는 가드

**3. In-component (컴포넌트 가드)**
- 컴포넌트 내에서만 적용되는 가드

---

### 라우팅 설계 (4/4): RESTful API

#### REST API

**"자원을 정의"하고 "자원에 대한 주소를 지정"하는 전반적인 방법을 서술**

> "각각 API 서버 구조를 작성하는 모습이 너무 다르니
> 어느정도 약속을 만들어서 다같이 통일해서 쓰자!"

#### REST 3요소

| 요소 | 설명 |
|------|------|
| **1. 자원의 식별** | URI |
| **2. 자원의 행위** | HTTP Methods |
| **3. 자원의 표현** | JSON 데이터 (궁극적으로 표현되는 데이터 결과물) |

**RESTful**
- REST 원리를 따르는 시스템을 RESTful 하다고 부름

---

## 4️⃣ 컴포넌트 분리 및 책임 정의

### 컴포넌트 분리 및 책임 정의 (1/2)

#### View 컴포넌트 vs UI 컴포넌트 구분

**Large Vue Project Structure**
```
src/
├── views/
│   ├── HomeView.vue
│   ├── AboutView.vue
│   └── DashboardView.vue
├── components/
│   ├── UI Components
│   │   ├── Button.vue
│   │   └── Modal.vue
│   └── Shared Components
│       ├── UserCard.vue
│       └── Pagination.vue
├── layouts/
│   ├── DefaultLayout.vue
│   └── AdminLayout.vue
└── store/
    ├── userStore.js
    └── productStore.js
```

#### 핵심 원칙

**1. 하나의 컴포넌트는 하나의 역할만 책임**

**2. Props/Emits 설계**
- 데이터의 소유 주체를 명확히 하기

---

### 컴포넌트 분리 및 책임 정의 (2/2)

#### 실제 프로젝트 예시

```
App.vue
├── NavBar (component)
└── RouterView
    ├── LandingView
    ├── ThreadListView
    │   ├── PopularThreads (component)
    │   ├── ThreadList (component)
    │   │   └── ThreadItem (component)
    │   └── LoadingSpinner (component)
    ├── ThreadDetailView
    │   ├── ThreadDetail (component)
    │   │   ├── LikeButton (component)
    │   │   └── GoogleMaps (component)
    │   ├── CommentForm (component)
    │   └── CommentList (component)
    │       └── CommentItem (component)
    ├── ThreadWriteView
    │   ├── ThreadWrite (component)
    │   └── ThreadForm (component)
    │       └── AIFeedBack (component)
    ├── BookListView
    │   ├── BestSellers (component)
    │   ├── AIRecommendations (component)
    │   ├── CategoryBooks (component)
    │   └── BookCard (component)
    └── BookDetailView
        ├── BookDetail (component)
        │   └── BookCard (component)
        ├── LoadingSpinner (component)
        └── Recommendations (component)
```

**데이터 흐름:**
- `thread: Object (update)`
- `book: Object`
- `category: Object`
- `comment: Object`

---

## 5️⃣ API 설계 및 연동 구조

### API 설계 및 연동 구조 (1/2)

#### 주요 고려사항

**1. Back-End API 문서화**
- 응답 데이터 명세서 작성

**2. AJAX 기술 결정**
- 공통 인스턴스 설정 (API_URL, headers 등)

**3. 올바른 예외 / 에러 처리**

---

### OpenAPI Specification (OAS)

**RESTful API를 설명하고 시각화하는 표준화된 방법**

**API에 대한 세부사항을 기술할 수 있는 공식 표준**

---

### API 설계 및 연동 구조 (2/2)

#### 적용 전후 비교

**Before:**
```
존재하지 않는 게시글 조회 시
상태 코드 500 응답
```

**After:**
```
존재하지 않는 게시글 조회 시
상태 코드 404 응답
```

**참고:**
- 응답 데이터 명세서를 작성하여 프론트엔드-백엔드 간 명확한 계약 수립
- 공통 인스턴스 설정으로 일관된 API 호출 구조 유지

---

## 6️⃣ UI/UX 스타일 가이드

### 스타일 가이드 중요성

**일관된 디자인 시스템 구축**
- 색상 팔레트
- 타이포그래피
- 컴포넌트 스타일
- 반응형 디자인 가이드라인

**협업 효율성 증대**
- 디자이너와 개발자 간 원활한 소통
- 재사용 가능한 UI 컴포넌트 구축

---

## 7️⃣ Git Flow

### Git Flow 전략

**브랜치 전략 수립**
- `main`: 프로덕션 브랜치
- `develop`: 개발 브랜치
- `feature/*`: 기능 개발 브랜치
- `hotfix/*`: 긴급 버그 수정 브랜치

**협업 규칙**
- Pull Request 리뷰 프로세스
- 코드 리뷰 가이드라인
- 머지 규칙

---

## 8️⃣ 문서화

### 문서화의 중요성

**프로젝트 문서 작성**
- README.md
- API 문서
- 컴포넌트 문서
- 개발 환경 설정 가이드

**유지보수성 향상**
- 새로운 팀원의 온보딩 시간 단축
- 프로젝트 이해도 향상

---

## 9️⃣ 데이터 가공 및 저장

### 데이터 전처리 과정

#### Step 1: 데이터 수집
- API에서 데이터 가져오기
- 파일에서 데이터 읽기

#### Step 2: 데이터 가공
- 필요한 필드만 추출
- 데이터 형식 변환
- 불필요한 데이터 제거

#### Step 3: 전처리 데이터 저장

---

### Step 3: 전처리 데이터 저장

#### books.json 생성

```python
output_file = "books.json"

with open(output_file, 'w', encoding="utf-8") as f:
    json.dump(books, f, ensure_ascii=False, indent=4)
```

---

### books.json 구조 예시

```json
{
  "model": "books.book",
  "pk": 2,
  "fields": {
    "category": 1,
    "title": "작별하지 않는다 - 2024 노벨문학상 수상작가, 한강 장편소설",
    "description": "2016년 채식주의자로 인터내셔널 부커상을 수상하고 2018년 흰으로 같은 상 최종 후보에 오른 한강 작가의 5년 만의 장편소설. 2019년 겨울부터 이듬해 봄까지 계간 문학동네에 전반부를 연재하여 큰 관심을 모은 작품이다.",
    "isbn": "9788954682152",
    "cover": "https://image.aladin.co.kr/product/27877/5/coversum/8954682154_3.jpg",
    "publisher": "문학동네",
    "pub_date": "2021-09-09",
    "author": "한강",
    "customer_review_rank": 9.5
  }
}
```

---

## 🔟 참고: Q 객체

### Q 객체란?

**DB에 저장된 수많은 게시글 데이터 중 특정 조건에 해당하는 정보를 조회하기 위한 도구**

---

### ORM (Object-Relational-Mapping)

**정의:**
객체 지향 프로그래밍 언어를 사용하여 호환되지 않는 유형의 시스템 간에 데이터를 변환하는 기술

#### ORM의 역할

```
Python Object  ←→  Django ORM  ←→  SQL Database
```

**Django의 내장된 ORM이 중간에서 이를 해석**

---

### QuerySet API

**ORM에서 데이터를 검색, 필터링, 정렬 및 그룹화 하는 데 사용하는 도구**

**API를 사용하여 SQL이 아닌 Python 코드로 데이터를 처리**

```
Django Object  →  QuerySet API  →  ORM  →  SQL  →  Database
                                           ↓
                          QuerySet or Instance
```

---

### QuerySet API 구문 동작 예시

```python
Article.objects.all()
```

**동작 흐름:**
1. Django Object에서 요청: "전체 게시글 줘"
2. ORM이 SQL로 변환
3. Database에서 전체 게시글 데이터 반환
4. QuerySet 형태로 받음

---

### Field Lookups

**Query에서 조건을 구성하는 방법**

**QuerySet 메서드 `filter()`, `exclude()` 및 `get()`에 대한 키워드 인자로 지정됨**

#### Field Lookups 예시

```python
# 내용에 'dja'가 포함된 모든 게시글 조회
Article.objects.filter(content__contains='dja')

# 제목이 'he'로 시작하는 모든 게시글 조회
Article.objects.filter(title__startswith='he')
```

**참고:** [Django Field Lookups 공식 문서](https://docs.djangoproject.com/en/4.2/ref/models/querysets/#field-lookups)

---

### Q 객체의 필요성

**문제:**
```
내용에 'dja'가 포함 AND 제목이 'he'로 시작
두 조건 모두 만족하는 게시글을 조회하기 위한 ORM문은?
```

**해결책: Q 객체**

**Q()** - Django ORM에서 복잡한 조건을 구성할 때 사용하는 도구

---

### Q 객체를 통한 ORM 조건 구성

#### 1단계: Q 객체 생성

```python
from django.db.models import Q

Q(content__contains='dja')
Q(title__startswith='he')
```

---

#### 2단계: 비트 논리 연산자를 활용한 조건문 작성

| 연산자 | 의미 |
|--------|------|
| `&` | AND |
| `|` | OR |
| `~` | NOT |

---

### AND 조건

**filter() 메서드 내 두 Q 객체를 `&`로 연결**

```python
def and_query(request):
    articles = Article.objects.filter(
        Q(content__contains='dja') & Q(title__startswith='he')
    )
    serializer = ArticleSerializer(articles, many=True)
    return Response(serializer.data)
```

**결과:**
```
'내용에 dja가 포함' AND '제목이 he로 시작'하는 Article 조회
```

```json
GET /articles/and/
HTTP 200 OK

[
  {
    "id": 1,
    "title": "heto",
    "content": "This article contains django snippet for demonstration."
  },
  {
    "id": 2,
    "title": "heaven",
    "content": "Another article with django inside the content."
  }
]
```

---

### OR 조건

**filter() 메서드 내 두 Q 객체를 `|`로 연결**

```python
def or_query(request):
    articles = Article.objects.filter(
        Q(content__contains='dja') | Q(title__startswith='he')
    )
    serializer = ArticleSerializer(articles, many=True)
    return Response(serializer.data)
```

**결과:**
```
'내용에 dja가 포함' OR '제목이 he로 시작'하는 Article 조회
```

```json
GET /articles/or/
HTTP 200 OK

[
  {
    "id": 1,
    "title": "heto",
    "content": "This article contains django snippet for demonstration."
  },
  {
    "id": 2,
    "title": "heaven",
    "content": "Another article with django inside the content."
  },
  {
    "id": 3,
    "title": "hero",
    "content": "Content including Vue is present here."
  }
]
```

---

### NOT 조건

**filter() 메서드 내 Q 객체 앞에 `~` 추가**

```python
def not_query(request):
    articles = Article.objects.filter(
        ~Q(title__startswith='he')
    )
    serializer = ArticleSerializer(articles, many=True)
    return Response(serializer.data)
```

**결과:**
```
'제목이 he로 시작하지 않는' Article 조회
```

```json
GET /articles/not/
HTTP 200 OK

[
  {
    "id": 3,
    "title": "start",
    "content": "Article content without the key string."
  }
]
```

---

### 복합 조건 예제

**문제:**
```
내용에 'dja'를 포함하지 않고 제목이 'he'로 시작하는 Article
또는
제목이 'st'로 시작하는 Article을 조회하시오.
```

**풀이:**
```python
def complex_query(request):
    articles = Article.objects.filter(
        (~Q(content__contains='dja') & Q(title__startswith='he')) |
        Q(title__startswith='st')
    )
    serializer = ArticleSerializer(articles, many=True)
    return Response(serializer.data)
```

**해설:**
1. `~Q(content__contains='dja')` - 내용에 'dja' 포함하지 않음
2. `& Q(title__startswith='he')` - AND 제목이 'he'로 시작
3. `| Q(title__startswith='st')` - OR 제목이 'st'로 시작

---

## 📝 프로젝트 체크리스트

### 기획 단계
- [ ] 요구사항 분석 완료
- [ ] 핵심 기능 우선순위 설정
- [ ] 사용자 페르소나 정의
- [ ] 제약 사항 파악

### 설계 단계
- [ ] 아키텍처 결정
- [ ] 기술 스택 선정
- [ ] 프로젝트 구조 설계
- [ ] 라우팅 설계
- [ ] 컴포넌트 구조 설계
- [ ] API 설계
- [ ] UI/UX 가이드 수립

### 개발 단계
- [ ] Git Flow 전략 수립
- [ ] 코딩 컨벤션 정의
- [ ] 문서화 작성
- [ ] 데이터 전처리 및 저장

### 코드 품질
- [ ] 컴포넌트 단일 책임 원칙 준수
- [ ] Props/Emits 명확한 정의
- [ ] API 에러 처리
- [ ] 코드 리뷰 프로세스

---

## 🎯 핵심 포인트 요약

### 1. 기획과 설계
- **요구사항을 명확히 정의**하고 우선순위를 설정
- **아키텍처와 기술 스택**을 신중히 선택
- **일관된 컨벤션**으로 협업 효율성 확보

### 2. Vue 프로젝트
- **라우팅 설계**: 중첩 라우팅과 가드 전략
- **컴포넌트 분리**: View vs UI 컴포넌트 구분
- **API 연동**: 명확한 문서화와 에러 처리

### 3. 데이터 처리
- **전처리 과정**: 수집 → 가공 → 저장
- **Django ORM**: QuerySet API와 Q 객체 활용
- **복잡한 쿼리**: AND, OR, NOT 조건 조합

### 4. 협업
- **Git 컨벤션**: feat, fix, docs, style, refactor
- **문서화**: README, API 문서, 컴포넌트 문서
- **코드 리뷰**: Pull Request 프로세스

---

## 📚 참고 자료

- [Django Q 객체 공식 문서](https://docs.djangoproject.com/en/5.1/topics/db/queries/#complex-lookups-with-q)
- [Django Field Lookups](https://docs.djangoproject.com/en/4.2/ref/models/querysets/#field-lookups)
- [OpenAPI Specification](https://swagger.io/specification/)
- Vue Router 공식 문서
- Django REST Framework 공식 문서

---

## 💡 학습을 마치며

이 가이드는 웹 개발 프로젝트를 체계적으로 진행하기 위한 로드맵을 제공합니다.

**핵심은:**
1. **명확한 기획과 설계**
2. **일관된 코드 작성**
3. **효율적인 협업**
4. **철저한 문서화**

이 네 가지를 기억하며 프로젝트를 진행하세요! 🚀
